//Removes the leading and trailing white space and line terminator characters from a string.

var glue = "Glue is    here From Switzerland      .    "
let result = glue.trim();
console.log(result) // Glue is here From Switzerland. 
