//Finds the first substring match in a regular expression search.
var glue = "Glue is here From Switzerland."
console.log(glue.search('e')) //e


var glue = "Glue is here From Switzerland."
console.log(glue.search('is')) //5
